#include <stdio.h>
#include <math.h>
int main()
{
    double x;
    printf("Enter x { 3.3 <= x <= 4.65 } -> ");
    scanf("%lf", &x);
    if (3.3 <= x && x <= 4.65)
    {
      double y = asin(4.0 - x) + log(log(x));
      double z = acos((2.0 * y) / (1.0 + y));
      printf("y(x) = %lf\nz(y) = %lf\n", y, z);
    }
    else
      printf("x value is incorrect!\n");
    return 0;
}
